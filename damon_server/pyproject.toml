[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "damon-server"
version = "0.1.0"
description = "Damon Agent Backend API Server"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Damon Agent Team", email = "team@damon-agent.dev"}]
maintainers = [{name = "Damon Agent Team", email = "team@damon-agent.dev"}]
keywords = ["damon-agent", "api", "backend", "fastapi", "websocket"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Operating System :: OS Independent",
]
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.109",
    "uvicorn>=0.27",
    "gunicorn>=21.0",
    "pydantic>=2.6",
    "pydantic-settings>=2.2",
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "asyncpg>=0.29",
    "redis>=5.0",
    "python-jose[cryptography]>=3.3",
    "passlib[bcrypt]>=1.7",
    "python-multipart>=0.0.6",
    "httpx>=0.26",
    "websockets>=12.0",
    "aiofiles>=23.1",
    "slowapi>=0.1",
    "prometheus-client>=0.19",
    "structlog>=24.1",
    "python-dateutil>=2.8",
    "pyyaml>=6.0",
    "damon-license>=0.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
    "pytest-asyncio>=0.23",
    "pytest-cov>=4.1",
    "ruff>=0.3",
    "mypy>=1.8",
    "pre-commit>=3.6",
    "httpx>=0.26",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["damon_server*"]

[tool.ruff]
line-length = 120
target-version = "py311"
lint.select = ["E", "F", "I", "W", "UP", "B", "C4", "C90"]
lint.ignore = ["E501", "B008"]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-n auto --tb=short"